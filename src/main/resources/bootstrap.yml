spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - namespace: amq
            name: demo-config
      reload:
        enabled: true
        mode: event
        strategy: refresh

management:
  endpoint:
    restart:
      enabled: true
